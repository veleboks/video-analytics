postgres:
  dsn: "host=postgres port=5432 user=vidanalytics password=secret dbname=runner sslmode=disable"

minio:
  endpoint: "http://minio:9000"
  access_key: "minio-access-key"
  secret_key: "minio-secret-key"

kafka:
  brokers:
    - "kafka:9092"
  group_id: "video-runner-group"
  scenario_topic: "video-scenarios"
  heartbeat_topic: "heartbeats"

detection:
  endpoint: "http://detection:8004"
